# Example environment variables for rustar-api

# Server configuration
API_SERVER_HOST=127.0.0.1
API_SERVER_PORT=8080

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Choose one of the following configurations:

# --- Option 1: Local PostgreSQL (via Docker) ---
# Uncomment these lines for local development with docker-compose
# API_DATABASE_URL=postgresql://postgres:postgres@localhost:5433/rustar-api
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/rustar-api
# API_SKIP_MIGRATIONS=false

# --- Option 2: Supabase (Production/Remote) ---
# Uncomment these lines to use Supabase (replace [YOUR_PASSWORD] with actual password)
API_DATABASE_URL=postgresql://postgres:[YOUR_PASSWORD]@db.gxrcklaazsihvgbxxddy.supabase.co:5432/postgres?sslmode=require
DATABASE_URL=postgresql://postgres:[YOUR_PASSWORD]@db.gxrcklaazsihvgbxxddy.supabase.co:5432/postgres?sslmode=require
# Set to true if Supabase already has migrations applied (skip running migrations on app startup)
API_SKIP_MIGRATIONS=true

# Database pool size
API_DATABASE_POOL_SIZE=5

# Message broker (MQTT) configuration
API_MESSAGE_BROKER_HOST=127.0.0.1
API_MESSAGE_BROKER_PORT=1883
API_MESSAGE_BROKER_KEEP_ALIVE=60
